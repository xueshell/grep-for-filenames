== grep-for-filenames ==

A bash script to list filenames from a directory whose basenames are not referenced in text files of another directory. Searches are recursive in both directories.

=== Usage ===

 ./grep-for-filenames DIRECTORY1 DIRECTORY2

Search for filenames from DIRECTORY1 in DIRECTORY2.
